# 花海画布 2026.08.01.4 发布验证

验证时间：2026-07-31（Asia/Shanghai）

## 结论

当前候选版本通过自动化、可见 Edge 实机、真实 API 媒体与 Gemini / GPT 四参考图接力验证。

## 自动化与服务

- Python 3.12.10，解释器：`F:\huabu\.venv\Scripts\python.exe`
- pytest：55 项全部通过
- 发布烟雾：23 项全部通过
- 应用版本：`2026.08.01.4`
- 服务地址：`http://127.0.0.1:3000/`
- 更新源：仅 `xy2446522127-code/miora-studio`

## 可见 Microsoft Edge 实机

- 1920 × 1080
- 1672 × 941（设计参考精确视口）
- 1440 × 900
- 1280 × 720
- 720 × 900

首页、项目管理、智能画布、批量连接、API 生成、插件生成、生成素材栏与插件中心均完成参考图 / 实际页面并排比对。所有视觉视口报告均满足：无横向溢出、无控制台错误、项目固定网格加载成功、画布测试节点加载成功、批量选择状态可见。

## 交互回归

- 智能画布交互报告 11 项断言全部通过
- 主壳层 12 个页面全部真实加载并有内容
- API / 插件模式切换与图片 / 视频切换正常
- 参考图、批量连接接口、常态 / 选中连线、明确缩放百分比正常
- 编辑面板内 `Ctrl + 滚轮` 不再误缩放画布
- 低动态效果、深浅主题和中英文切换正常
- 浏览器控制台错误：0

## 真实媒体验证

- 对话：OpenAI 兼容接口真实响应成功
- 图片：真实生成成功并保存到本地输出目录
- 视频：真实生成成功，H.264、1920 × 1080、8 秒
- 上游接口返回余额不足时保持真实失败状态，未伪装为成功

## Gemini / GPT 四参考图验证

两款浏览器辅助插件均在用户已登录的可见 Microsoft Edge 中完成：

1. 将 4 张原始参考图分别作为独立附件上传
2. 官网确认附件数为 4
3. 用户提示词逐字写入并回读校验，不改写、不追加隐藏风格提示
4. 提交官网生成
5. 下载生成结果
6. 自动复制到花海画布本地输出并作为画布素材返回

Gemini 实测输出为 1375 × 768 PNG；GPT 实测输出为 1672 × 941 PNG。结构化证据见 `docs/screenshots/2026.08.01.4/plugin-browser-qa.json`。

本轮同时修复了 Edge 休眠标签页仍暴露无障碍控件、展开菜单与叶子菜单项模式不同、PowerShell 严格模式单元素集合等真实环境兼容问题。

## 隐私与发布边界

- API Key、`.env`、`.venv/`、`data/local/`、`assets/`、浏览器资料、Cookie、插件任务和真实生成媒体均不进入 Git
- 仓库只提交源码、测试、设计参考、可公开的 QA 截图和脱敏验证报告
- 保留原始许可证、上游作者归属与当前开发者署名
