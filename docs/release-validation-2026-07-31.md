# 花海画布 2026.07.31.1 发布验证

## 兼容性基线

- 对照上游 `hero8152/Infinite-Canvas` 当前 `main` 分支检查后端接口：上游 133 个、花海画布 143 个，上游接口缺失数为 0。
- 对照 13 个原有页面检查 DOM ID 与具名 JavaScript 功能入口；原有 ID 均保留。
- 项目卡片拖动入口按产品要求移除，项目管理改为按更新时间从左到右、从上到下固定排序。

## 双画布验证

- 普通画布与智能画布均验证：节点创建、框选、多选、批量连接代理端口、兼容性提示、撤销与保存。
- 普通与智能画布均保留完整 API/视频节点输入、提示词和媒体拖放区、插件视频来源、成果分类轨。
- 连线验证包含普通流动态、选中关系高亮态和无关关系弱化态。
- 空白画布无倒影；智能建议面板已移除。
- 智能画布新增实时缩放百分比、100% 复位、适配视图和 `Ctrl + 滚轮` 提示。
- 两套画布均恢复可视媒体在正常缩放下的高分辨率显示。

## 自动化与浏览器回归

- Python 单元测试：34 项通过。
- JavaScript 语法检查：普通画布与智能画布通过。
- 15 个产品页面在 1440×900 下完成浏览器回归：无页面错误、控制台错误、失败资源或横向溢出。
- 智能画布在 1920×1080 和 900×760 下验证缩放提示可见，复位后准确显示 100%。

## 模型与外部服务

- 对话：自定义 API 对话请求成功并返回内容。
- 图片：本地调用链工作正常；上游返回余额不足，未获得生成结果。
- 标准视频：本地调用链工作正常；上游站点未提供兼容视频路由。
- 插件视频：插件适配调用链工作正常；上游返回当前无可用视频通道。
- API Key、Cookie、用户媒体、日志、`.env`、本地配置和 `.venv` 均不进入 Git。

## 本地启动与更新

- `run.bat` 仅调用 `F:\huabu\.venv\Scripts\python.exe`，并已验证中文路径下冷启动。
- 桌面快捷方式使用花海 Logo，目标为 `F:\huabu\run.bat`。
- 本地演示地址为 `http://127.0.0.1:3000/`，不需要登录。
- 更新源仅指向 `xy2446522127-code/miora-studio`；启动后不会改写受 Git 管理的源文件。
