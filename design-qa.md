# 花海画布 2026.08.01.3 设计 QA

## 结论

**passed**

已按最终确认的「潮汐玻璃」效果图，对首页、项目管理、智能画布、批量连接、API / 插件生成、生成素材栏与插件中心完成同视口、同状态对照。当前未发现阻止发布的 P0、P1 或 P2 视觉缺陷。

## 对照基线

- 最终参考图：`docs/design-reference/huahai-final-2026-07-31/`
- 实机截图：`docs/screenshots/2026.08.01.3/actual-1672x941/`
- 左右合并对照：`docs/screenshots/2026.08.01.3/comparisons-1672x941/`
- 对照环境：可见 Windows Chrome，1672 × 941，深色主题，真实应用路由与真实 DOM 状态

## 覆盖页面

1. 首页
2. 项目管理
3. 智能画布
4. 多选节点批量连接
5. API 生成面板
6. 插件生成面板
7. 生成素材侧栏
8. 插件中心与插件制作说明

## 已修复的差异

- P1：生成面板曾为横向紧凑布局，与效果图的纵向玻璃面板不一致。已改为与参考图一致的双标签、提示词、参考素材、提供方、模型、规格、质量、数量和珊瑚色主按钮结构。
- P1：生成面板曾处于缩放世界坐标中，跟随画布缩放并可能被裁切。已挂载到应用壳层，保持固定尺寸与可用点击区域。
- P1：1280 × 720 下生成面板底部曾超出视口。已增加低高度紧凑规则和内部滚动，并验证运行按钮完整可见。
- P1：底部缩放 HUD 曾覆盖生成面板主按钮。已修正层级。
- P2：项目卡片曾可拖动且排列不稳定。已改为按更新时间从左到右、从上到下的固定四列序列，并保留参考图中的克制倒影。
- P2：普通与选中连接线状态不够清楚。已实现低密度常态流动和关系线高亮流动，低动态模式关闭动画。
- P2：窄屏插件中心内容层级拥挤。已改为单列卡片与纵向文档流，所有操作仍可见。
- P1：产品壳层曾隐藏在线生图、GPT 对话和本地工具组。已恢复全部原功能导航，并保持与花海视觉系统一致。
- P1：普通画布旧链接仍可直接进入。现已统一重定向到智能画布，同时兼容旧查询参数与书签。
- P1：更新弹窗残留 ModelScope 来源选择。已与后端统一为花海画布 GitHub 单一更新源。

## 允许的内容差异

- 项目名称、时间、节点、素材缩略图和生成结果来自实时本地数据，不使用效果图中的假数据。
- 插件生成列表只显示当前已安装且声明对应媒体能力的插件，因此数量随插件目录动态变化，不固定写死 Gemini / GPT。
- 实机画布保留现有节点数据、端口与工作流语义；布局和视觉遵循参考图，但不伪造不存在的业务结果。

## 视口与交互验证

- 1920 × 1080
- 1672 × 941（参考图精确视口）
- 1440 × 900
- 1280 × 720
- 720 × 900

所有截图均无水平溢出和控制台错误。交互回归覆盖生成面板标签切换、图片/视频切换、动态插件列表、Ctrl + 滚轮百分比缩放、面板滚轮隔离、批量连接接口、常态/选中连接流、低动态模式、生成结果侧栏和插件中心。主壳层另以可见 Chrome 逐一打开 12 个功能页面，并验证主题及中英文状态在父页面与功能页之间同步。
